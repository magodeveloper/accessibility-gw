# Environment Configuration for k6 Load Tests - TEMPLATE
# ⚠️ IMPORTANTE: Este es un archivo de ejemplo
# 
# PARA DESARROLLO:
#   cp .env.example .env.development
#   Editar .env.development con valores locales
#
# PARA PRODUCCIÓN:
#   cp .env.example .env.production
#   Configurar .env.production con valores reales y seguros
#
# El script manage-load-tests.ps1 cargará automáticamente:
#   - .env.development por defecto
#   - .env.production con flag -Production

# Base URLs
BASE_URL=http://localhost:8100
HEALTH_URL=http://localhost:8100/health

# Load Test Configuration
LIGHT_USERS=10
LIGHT_DURATION=2m

NORMAL_USERS=50
NORMAL_DURATION=5m

HEAVY_USERS=100
HEAVY_DURATION=10m

STRESS_USERS=200
STRESS_DURATION=5m

# Timing Configuration
SLEEP_MIN=0.5
SLEEP_MAX=2.0

# Output Configuration
VERBOSE=false
OUTPUT_FORMAT=json

# Thresholds Override
MAX_ERROR_RATE=0.01
MAX_P95_DURATION=500
MIN_THROUGHPUT=10

# Backend Services (for integration tests)
USERS_SERVICE_URL=http://localhost:8081
ANALYSIS_SERVICE_URL=http://localhost:8082
REPORTS_SERVICE_URL=http://localhost:8083

# Monitoring
PROMETHEUS_URL=http://localhost:9090
GRAFANA_URL=http://localhost:3010

# Test Data
TEST_DATA_SIZE=100
LARGE_PAYLOAD_SIZE=5000

# Security - JWT Configuration
# ⚠️ CRÍTICO: Cambia estos valores para tu entorno
# NUNCA uses claves de producción en desarrollo
# Para generar una nueva clave segura:
#   PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#   Node.js: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
API_KEY=your-test-api-key-here
JWT_SECRET=your-secret-key-here-min-64-chars-recommended-change-this-value
JWT_ISSUER=https://api.accessibility.company.com/users
JWT_AUDIENCE=https://accessibility.company.com
JWT_EXPIRATION_MINUTES=60

# Notas de Seguridad:
# - JWT_SECRET: Mínimo 32 caracteres, recomendado 64+
# - En producción usar secret manager (Azure Key Vault, AWS Secrets Manager)
# - JWT_ISSUER: URL del servicio emisor de tokens
# - JWT_AUDIENCE: URL de la audiencia/destinatario del token
# - JWT_EXPIRATION_MINUTES: Tiempo de expiración del token en minutos
