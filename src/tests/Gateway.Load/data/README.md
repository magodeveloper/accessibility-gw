# Test data and configuration files

This directory contains test data and configuration files for load testing.

## Structure

- `users.json` - Sample user data for testing
- `environments.json` - Environment-specific configurations
- `*.csv` - CSV files with test data for parameterized tests
